MainPrgm MyProgram ;
Var
    let x, y : Int ;
    let A, B : [Int; 10] ;
    @define Const MaxValue : Int = 100 ;
BeginPg
    { 
      x := 5 ;
      y := x + 10 ;
      if (x > 0) then { y := y - 1 ; } else { y := y + 1 ; }
      do { x := x - 1; } while (x > 0);
    }
EndPg ;
